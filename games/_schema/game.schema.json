{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://carrom-engine/games/_schema/game.schema.json",
  "title": "Carrom games/ manifest",
  "$comment": "Validates ONE manifest.json under res://games/ in isolation. The runtime loader (scripts/ui/menu/FlavourLoader.cs) walks the tree and cascades manifest fields + asset lookups leaf -> ancestors -> engine defaults; that cascade is NOT visible to the schema. The same schema covers three node shapes: (1) engine root games/manifest.json (sparse, no rule_set, no UI), (2) group nodes (title/tagline/accent only \u2014 clicking descends into games/<id>/), (3) leaf nodes (carry rule_set: 'icf' | 'casual'; icf = tournament, casual = relaxed carrom). All three shapes overlap; every field below is optional so a sparse manifest validates. The runtime decides which fields it actually consumes based on rule_set.",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "id": {
      "type": "string",
      "description": "Stable identifier. Must equal the folder name for arcade leaves. Defaults to the folder name when omitted."
    },
    "title": {
      "type": "string",
      "description": "Display name on the carousel / picker."
    },
    "tagline": {
      "type": "string",
      "description": "One-liner under the title."
    },
    "accent": {
      "type": "string",
      "pattern": "^#[0-9A-Fa-f]{6}$",
      "description": "Brand colour driving badges, hover glows, indicator chips."
    },
    "poster": {
      "type": "string",
      "description": "Optional carousel-card path. Convention default: poster.png at the node root, falling back to posters/main.png."
    },
    "card_color": {
      "type": "string",
      "pattern": "^#[0-9A-Fa-f]{6}$",
      "description": "Row-1 carousel card gradient base colour. Absent \u2192 the accent. Read by FlavourLoader -> Node.CardColor (casino uses this)."
    },
    "card_texture": {
      "type": "string",
      "description": "Row-1 carousel card background texture path. Absent \u2192 the accent gradient. Read by FlavourLoader -> Node.CardTexturePath (purist uses this)."
    },
    "rule_set": {
      "type": "string",
      "enum": [
        "icf",
        "casual"
      ],
      "description": "Required on a playable LEAF. icf (tournament) / casual (relaxed) — built by RulesFlavour. Absent on group nodes (descend-into-children instead of play)."
    },
    "rules": {
      "type": "object",
      "additionalProperties": true,
      "description": "Per-flag deltas relative to the rule_set preset (scripts/game/RuleFlags.cs). Every field nullable; missing keys inherit the preset.",
      "properties": {
        "queen_cover_required": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "early_queen_returns": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "last_coin_before_queen_loses_board": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "no_contact_policy": {
          "type": [
            "string",
            "null"
          ],
          "enum": [
            "none",
            "warn",
            "immediate",
            null
          ],
          "description": "Case-insensitive at runtime; canonical lowercase here."
        },
        "opp_pocket_is_foul": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "queen_is_standard_coin": {
          "type": [
            "boolean",
            "null"
          ],
          "description": "True: queen has no cover requirement, no early-queen return, no special turn-flow side effects — treated as an ordinary coin."
        },
        "coin_off_board_is_foul": {
          "type": [
            "boolean",
            "null"
          ],
          "description": "True (ICF): a non-striker coin jumping off the board is a foul \u2014 returned to centre AND the shooter loses one own coin (or owes a due) and the turn passes. False (Casual): the coin just returns, no penalty."
        }
      }
    },
    "board_size": {
      "type": "string",
      "description": "BoardSizeRegistry id. Defaults to 'icf' for rules leaves."
    },
    "leaderboard_id": {
      "type": "string",
      "description": "Steam leaderboard partition id. Defaults to the flavour id."
    },
    "surface_material": {
      "type": "string",
      "description": "icf/casual leaves \u2014 PbrTextureSetRegistry id for the playing surface."
    },
    "surface_tint": {
      "type": "string",
      "pattern": "^#[0-9A-Fa-f]{6}$",
      "description": "Optional #RRGGBB multiply over the playing-surface albedo (e.g. tint an ivory/felt set navy for a slate look). Absent \u2014 the wood set's own tone."
    },
    "frame_material": {
      "type": "string",
      "description": "icf/casual leaves \u2014 PbrTextureSetRegistry id for the frame border."
    },
    "surface_material_id": {
      "type": "string",
      "description": "Alias for surface_material."
    },
    "frame_material_id": {
      "type": "string",
      "description": "Alias for frame_material."
    },
    "leg_material": {
      "type": "string",
      "description": "PbrTextureSetRegistry id for the table legs. Defaults to the table/frame material."
    },
    "table_material": {
      "type": "string",
      "description": "PbrTextureSetRegistry id for the supporting wood table slab under the board."
    },
    "surface_roughness_scale": {
      "type": "number",
      "description": "Multiplier on the playing-surface roughness. Default 1.0."
    },
    "corner_albedo_scale": {
      "type": "number",
      "description": "Albedo multiplier on the rounded frame corner band so it tone-matches the straight rails. Default 0.82 (mahogany); a uniform dark frame wants ~1.0. Flavour.CornerAlbedoScale."
    },
    "corner_cutoff_mm": {
      "type": "number",
      "description": "Distance in mm along the mitre diagonal to the corner cut line. Default 20. Drives FrameBuilder's corner geometry."
    },
    "corner_round_inner": {
      "type": "boolean",
      "description": "Round (fillet) the inner edge of the frame corner. Default true."
    },
    "corner_round_outer": {
      "type": "boolean",
      "description": "Round (fillet) the outer edge of the frame corner. Default true."
    },
    "outer_bullnose": {
      "type": "boolean",
      "description": "Bullnose the frame's outer face. Default true (off when a surround abuts the outer face). FrameOuterBullnose."
    },
    "reflection_probe": {
      "type": "boolean",
      "description": "Per-game graphics toggle: local box-projected reflection probe (sharp metal reflections). Absent \u2192 off (flavours use the soft sky IBL). IFlavour.WantsReflectionProbe."
    },
    "ambient_dust": {
      "type": "boolean",
      "description": "Per-game graphics toggle: drifting atmosphere dust motes over the table. Default true (also needs Medium+ graphics + a backdrop). IFlavour.WantsAmbientDust."
    },
    "surface_roughness": {
      "type": "number",
      "description": "Playing-surface roughness (lower = glossier, 1.0 = fully matte / no shine). Default 0.30. Flavour.SurfaceRoughness."
    },
    "surface_uv_repeats": {
      "type": "number",
      "description": "Surface texture UV repeat count, overriding the ~8cm wood-grain auto-tiling. Few large repeats = one stone/marble slab; 0/unset = auto. Flavour.SurfaceUvRepeats."
    },
    "grain_anisotropy": {
      "type": "number",
      "description": "Anisotropic stretch of the surface grain reflections. Default 0.90. Fed to the surface shader's grain_anisotropy."
    },
    "surface_ao_strength": {
      "type": "number",
      "description": "Ambient-occlusion strength for the surface micro-structure. Default 1.0. Scales the AO applied by PlayingSurfaceBuilder."
    },
    "frame_finish": {
      "type": "string",
      "enum": [
        "satin",
        "gloss",
        "piano_gloss",
        "matte",
        "painted"
      ],
      "description": "Frame lacquer finish. 'piano_gloss' = mirror clearcoat, 'matte'/'painted' = no sheen, 'satin'/'gloss' = default soft clearcoat. Default satin."
    },
    "frame_grain_rotate": {
      "type": "boolean",
      "description": "Rotate the frame wood grain 90\u00b0. Set true when the frame_material texture's grain runs along its V (short) axis instead of U, so the grain reads lengthwise on the rails. Default false."
    },
    "corner_style": {
      "type": "string",
      "description": "Frame corner treatment, e.g. 'round_corners' for filleted+bullnosed corners. Default mitred square."
    },
    "corner_cap_mesh": {
      "type": "string",
      "description": "Shared corner-cap GLB in the engine root library res://assets/meshes/ (e.g. 'half_corner_cap.glb' / 'full_corner_cap.glb'); geometry only, surfaced via corner_cap_material. Absent \u2192 the flavour's own assets/meshes/corner_cap.glb."
    },
    "corner_cap_material": {
      "type": "string",
      "description": "Texture-set id (e.g. 'leather', 'brass') applied to the corner cap as a triplanar PBR material at runtime. Absent \u2192 the cap keeps its own baked material."
    },
    "corner_cap_material_scale": {
      "type": "number",
      "description": "Triplanar tiling scale for the corner-cap material (smaller = bigger grain). Default 0.3."
    },
    "corner_cap_material_normal": {
      "type": "number",
      "description": "Normal-map strength for the corner-cap material (punch the leather pores / brass brushing). Default 1.0."
    },
    "environment": {
      "type": "string",
      "description": "LightingPreset / environment id (e.g. 'cafe'). Drives lights + sky + table furniture. The optional `lighting` block overrides its light rig field-by-field."
    },
    "lighting": {
      "type": "object",
      "additionalProperties": false,
      "description": "Full lighting rig authored in-manifest, merged OVER the `environment` preset (unset fields inherit it). Rotations are [x,y,z] degrees, sizes [w,h] cm, colours #RRGGBB.",
      "properties": {
        "main_color":    { "type": "string", "description": "Key directional light colour (#RRGGBB)." },
        "main_energy":   { "type": "number", "description": "Key directional light energy." },
        "main_rotation": { "type": "array", "items": { "type": "number" }, "minItems": 3, "maxItems": 3, "description": "Key light Euler rotation [x,y,z] deg." },
        "ambient_color": { "type": "string", "description": "Ambient light colour (#RRGGBB)." },
        "ambient_energy":{ "type": "number", "description": "Ambient light energy." },
        "fog_density":   { "type": "number", "description": "Depth-fog density (0 = none)." },
        "fill":          { "type": "object", "additionalProperties": false, "description": "Soft secondary directional. Present replaces the preset fill; omit to inherit.", "properties": { "color": { "type": "string" }, "energy": { "type": "number" }, "rotation": { "type": "array", "items": { "type": "number" } } } },
        "spot":          { "type": "object", "additionalProperties": false, "description": "Overhead spot.", "properties": { "color": { "type": "string" }, "energy": { "type": "number" }, "rotation": { "type": "array", "items": { "type": "number" } }, "range": { "type": "number" }, "angle": { "type": "number" } } },
        "area":          { "type": "object", "additionalProperties": false, "description": "Soft overhead rectangular area light.", "properties": { "color": { "type": "string" }, "energy": { "type": "number" }, "size": { "type": "array", "items": { "type": "number" } }, "height": { "type": "number" }, "rotation": { "type": "array", "items": { "type": "number" } } } },
        "procedural_sky":{ "type": "object", "additionalProperties": false, "description": "Gradient sky (outdoor).", "properties": { "top": { "type": "string" }, "horizon": { "type": "string" }, "ground_bottom": { "type": "string" } } }
      }
    },
    "reflection_floor": {
      "type": "string",
      "pattern": "^#[0-9A-Fa-f]{6}$",
      "description": "Hex colour: warm minimum applied to the backdrop REFLECTION only (so polished metal reads gold in a dark room). Absent = off (black)."
    },
    "coin_profile": {
      "type": "string",
      "description": "CoinBuilderRegistry id for the coin body geometry: 'wood' (default, ICF/Casual) or 'deluxe' (enamel/brass chip coins used by themed casual leaves)."
    },
    "chair_yaw": {
      "type": "number",
      "description": "Seat-chair yaw in degrees. Default 0."
    },
    "chair_scale": {
      "type": "number",
      "description": "Seat-chair size multiplier. Default 1.0."
    },
    "backdrop": {
      "type": "string",
      "description": "Optional gameplay panorama path override. Convention default (all leaves): assets/images/backdrop.{jpg,png} at the node root."
    },
    "turn_marker": {
      "type": "object",
      "additionalProperties": false,
      "description": "Current-player marker tuning. All fields optional.",
      "properties": {
        "glb": {
          "type": "string",
          "description": "Marker mesh override. Convention default: assets/meshes/turn_marker.glb (else the rupee coin)."
        },
        "scale": {
          "type": "number",
          "description": "Size multiplier on top of the auto-fit. Default 1.0."
        },
        "offset_h": {
          "type": "number",
          "description": "mm outward(+)/inward(-) along the board radial. Default 0."
        },
        "offset_y": {
          "type": "number",
          "description": "mm up(+)/down(-) on the frame. Default 0."
        },
        "rotation": {
          "type": "number",
          "description": "Degrees about Y, on top of facing the board centre. Default 0."
        }
      }
    },
    "grunge": {
      "type": "object",
      "additionalProperties": false,
      "description": "Unified grunge. coins/frame = procedural finishes (no map covers coins or the frame). board/table = grunge-map channels driving surface_grime.png / table_grime.png: per-channel intensity multipliers (R=patina G=grime B=powder A=wear, default 1.0) + the patina tint range, lerped patina_min_color\u2192patina_max_color by the map's R amount. Default block lives in the root manifest; ICF overrides board/table to zero (pristine).",
      "properties": {
        "coins": {
          "$ref": "#/definitions/agingPair"
        },
        "frame": {
          "$ref": "#/definitions/agingPair"
        },
        "board": {
          "$ref": "#/definitions/grimeChannels"
        },
        "table": {
          "$ref": "#/definitions/grimeChannels"
        }
      }
    },
    "table_height": {
      "type": "number",
      "description": "Table-leg length in mm (= table height). Default 1100. The legs are this tall below the board."
    },
    "table_drop": {
      "type": "number",
      "description": "Lower the table onto the backdrop's painted floor by this many mm (POSITIVE = table lower). Drops the whole rig + camera together; the finite-parallax backdrop floor shifts up to meet the table (needs Medium+ graphics for the parallax). Default 0."
    },
    "coins": {
      "$ref": "#/definitions/coinSet"
    },
    "markings": {
      "$ref": "#/definitions/markingSet"
    },
    "indicator_color": {
      "type": "string",
      "pattern": "^#[0-9A-Fa-f]{6}$"
    },
    "sfx": {
      "type": "object",
      "additionalProperties": true,
      "description": "Optional per-event SFX overrides. Convention default: assets/audio/sfx/<event>.{mp3,ogg,wav}, auto-discovered across the node cascade (group->game). Engine ships universal carrom SFX (coin_click, flick, pocketed); declare an entry here only to point at a non-canonical file.",
      "properties": {
        "coin_click": {
          "type": "string"
        },
        "flick": {
          "type": "string"
        },
        "pocketed": {
          "type": "string"
        },
        "star_hit": {
          "type": "string"
        },
        "giftbox_hit": {
          "type": "string"
        },
        "collectable_hit": {
          "type": "string"
        },
        "bonus": {
          "type": "string"
        }
      }
    },
    "tutorial": {
      "type": "object",
      "additionalProperties": true,
      "properties": {
        "steps": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Walkthrough step ids looked up in TutorialStepCatalog."
        }
      }
    },
    "hints": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/hintEntry"
      }
    },
    "corner_cap_color": {
      "type": "string",
      "pattern": "^#[0-9A-Fa-f]{6}$",
      "description": "Hue+tone hex multiplied over the corner-cap's grayscale grain (cafe brown). Absent = no tint."
    }
  },
  "definitions": {
    "agingPair": {
      "type": "object",
      "additionalProperties": true,
      "properties": {
        "wear": {
          "type": "number",
          "minimum": 0,
          "maximum": 1
        },
        "powder": {
          "type": "number",
          "minimum": 0,
          "maximum": 1
        },
        "grime": {
          "type": "number",
          "minimum": 0,
          "maximum": 1
        }
      }
    },
    "grimeChannels": {
      "type": "object",
      "additionalProperties": false,
      "description": "Per-channel grunge-map intensity multipliers (default 1.0; 0 = off) + patina tint range.",
      "properties": {
        "patina": {
          "type": "number",
          "description": "Patina (R) intensity."
        },
        "grime": {
          "type": "number",
          "description": "Grime (G) intensity."
        },
        "powder": {
          "type": "number",
          "description": "Powder (B) intensity \u2014 tint + height/displacement."
        },
        "wear": {
          "type": "number",
          "description": "Wear (A) intensity \u2014 polish."
        },
        "patina_min_color": {
          "type": "string",
          "pattern": "^#[0-9A-Fa-f]{6}$",
          "description": "Patina tint at low amount (light oxidation)."
        },
        "patina_max_color": {
          "type": "string",
          "pattern": "^#[0-9A-Fa-f]{6}$",
          "description": "Patina tint at high amount (heavy oxidation)."
        }
      }
    },
    "coinSet": {
      "type": "object",
      "additionalProperties": true,
      "description": "icf/casual leaves treat the whole block as optional; missing fields fall back to IFlavour.DefaultCoins.",
      "properties": {
        "white": {
          "type": "string",
          "pattern": "^#[0-9A-Fa-f]{6}$"
        },
        "black": {
          "type": "string",
          "pattern": "^#[0-9A-Fa-f]{6}$"
        },
        "queen": {
          "type": "string",
          "pattern": "^#[0-9A-Fa-f]{6}$"
        },
        "striker": {
          "type": "string",
          "pattern": "^#[0-9A-Fa-f]{6}$"
        },
        "roughness": {
          "type": "number",
          "minimum": 0,
          "maximum": 1
        },
        "metallic": {
          "type": "number",
          "minimum": 0,
          "maximum": 1
        },
        "polish": {
          "type": "number",
          "description": "Coin clearcoat polish amount. CoinSet.Polish; default 0."
        },
        "bevel": {
          "type": "string",
          "description": "'brass' | 'none' | 'flat' | '#hex'."
        },
        "finish": {
          "type": "string",
          "description": "'matte' | 'satin' | 'polished'."
        },
        "striker_body_style": {
          "type": "string",
          "description": "'brass' | 'wood' | 'plastic' | 'chrome' | \u2026."
        },
        "white_emblem": {
          "type": "string"
        },
        "black_emblem": {
          "type": "string"
        },
        "queen_emblem": {
          "type": "string"
        },
        "white_emblem_material": {
          "type": "string"
        },
        "black_emblem_material": {
          "type": "string"
        },
        "queen_emblem_material": {
          "type": "string"
        },
        "white_material": {
          "type": "string"
        },
        "black_material": {
          "type": "string"
        },
        "queen_material": {
          "type": "string"
        },
        "striker_material": {
          "type": "string"
        }
      }
    },
    "markingSet": {
      "type": "object",
      "additionalProperties": true,
      "properties": {
        "line_color": {
          "type": "string",
          "pattern": "^#[0-9A-Fa-f]{6}$"
        },
        "spot_color": {
          "type": "string",
          "pattern": "^#[0-9A-Fa-f]{6}$"
        },
        "pocket_cup_color": {
          "type": "string",
          "pattern": "^#[0-9A-Fa-f]{6}$"
        },
        "pocket_rim_trim": {
          "type": "string",
          "description": "Pocket rim trim: a hex colour (#RRGGBB paints a flat ring) OR a material id / texture path (\"brass\" / res://\u2026 seats a 3D metal rim)."
        },
        "board_edge_trim": {
          "type": "string",
          "description": "Board edge trim: a material id / texture path (\"brass\" / res://\u2026 seats a 3D metal chamfer). The hex-colour form is unused for this key."
        },
        "feather_mask": {
          "type": "boolean"
        },
        "baseline_mask": {
          "type": "boolean"
        },
        "pocket_corner_mask": {
          "type": "boolean"
        },
        "baseline_spot_mask": {
          "type": "boolean"
        },
        "rosette_mask": {
          "type": "boolean"
        }
      }
    },
    "hintEntry": {
      "type": "object",
      "additionalProperties": true,
      "required": [
        "id",
        "event"
      ],
      "properties": {
        "id": {
          "type": "string"
        },
        "event": {
          "type": "string"
        },
        "policy": {
          "type": "string",
          "enum": [
            "once",
            "once_per_match",
            "always"
          ]
        },
        "priority": {
          "type": "integer"
        }
      }
    }
  }
}
